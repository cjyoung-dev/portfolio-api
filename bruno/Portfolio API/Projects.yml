info:
  name: Projects
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/projects"
  params:
    - name: status
      value: active
      type: query
      disabled: true
    - name: status
      value: complete
      type: query
      disabled: true
    - name: status
      value: archived
      type: query
      disabled: true
    - name: status
      value: invalid
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
