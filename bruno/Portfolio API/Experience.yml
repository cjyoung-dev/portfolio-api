info:
  name: Experience
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/experience"
  params:
    - name: type
      value: education
      type: query
      disabled: true
    - name: type
      value: invalid
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
