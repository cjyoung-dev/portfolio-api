info:
  name: Skills
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/skills"
  params:
    - name: category
      value: frontend
      type: query
      disabled: true
    - name: category
      value: invalid
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
